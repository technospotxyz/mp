

MainActivity.java**

```java
package com.example.loginpage;

import androidx.appcompat.app.AppCompatActivity;
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

public class MainActivity extends AppCompatActivity {
    Button btnGoToSecond;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        btnGoToSecond = findViewById(R.id.btnGoToSecond);

        btnGoToSecond.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intent = new Intent(MainActivity.this, SecondActivity.class);
                startActivity(intent);
            }
        });
    }
}
```

**2. SecondActivity.java**

```java
package com.example.loginpage;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class SecondActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main2);
    }
}
```

**3. activity\_main.xml**

```xml
<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:gravity="center"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to the First Page Shiva!"
        android:textSize="22sp"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"/>

    <Button
        android:id="@+id/btnGoToSecond"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Go to Second Page"
        android:layout_below="@id/tvTitle"
        android:layout_centerHorizontal="true"/>
</RelativeLayout>
```

**4. activity\_main2.xml**

```xml
<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:padding="16dp"
    tools:context=".SecondActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="You've Arrived at the Second Page Shiva!"
        android:textSize="22sp"
        android:textColor="@android:color/holo_blue_dark"/>
</RelativeLayout>
```

---

### 📝 Steps of the Practical

1. **Create a new Android Project**

   * Open **Android Studio** → **New Project** → Select **Empty Activity** → Name it `LoginPage`.

2. **Setup MainActivity and SecondActivity**

   * `MainActivity` is created automatically.
   * Create a new Java class `SecondActivity.java`.

3. **Design UI Layouts**

   * Open `res/layout/activity_main.xml` → paste the fixed XML.
   * Create another layout `activity_main2.xml` → paste the second XML.

4. **Connect Button to Second Activity**

   * In `MainActivity.java`, we added an `Intent` inside `btnGoToSecond.setOnClickListener()` to switch pages.

5. **Declare SecondActivity in Manifest**
   Open `AndroidManifest.xml` and **add SecondActivity**:

   ```xml
   <activity android:name=".SecondActivity"></activity>
   ```


